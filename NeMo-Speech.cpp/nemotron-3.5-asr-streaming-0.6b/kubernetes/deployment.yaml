apiVersion: apps/v1
kind: Deployment
metadata:
  name: nemotron-asr
  labels:
    app: nemotron-asr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nemotron-asr
  template:
    metadata:
      labels:
        app: nemotron-asr
    spec:
      serviceAccountName: nemotron-asr
      containers:
        - name: nemotron-asr
          image: docker.io/moosestack/nemo-speech-cpp_nemotron-3.5-asr-streaming-0.6b_cpu-x86-64:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          startupProbe:
            tcpSocket:
              port: http
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 30
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 8Gi
